import { hasWindow } from "@react-typed-hooks/utils";

const {{ camelCase name }}: () => string | boolean = () =>
  hasWindow() ? "{{ camelCase name }}" : false;

export default {{ camelCase name }};
